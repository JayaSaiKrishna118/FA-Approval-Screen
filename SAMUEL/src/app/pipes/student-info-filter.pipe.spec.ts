import { StudentInfoFilterPipe } from './student-info-filter.pipe';

describe('StudentInfoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentInfoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
